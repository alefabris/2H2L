name: Post Weekly Board Game Poll

on:
  schedule:
    - cron: "0 9 * * 4"  # Every Thursday at 09:00 UTC
  workflow_dispatch:

jobs:
  post_poll:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Read game list and send poll
        id: send_poll
        run: |
          GAMES=$(jq -r '.[]' games/index.json)
          EMOJIS=(🇦 🇧 🇨 🇩 🇪 🇫 🇬 🇭 🇮 🇯 🇰 🇱 🇲 🇳 🇴 🇵 🇶 🇷 🇸 🇹 🇺 🇻 🇼 🇽 🇾 🇿)

          MSG="🎲 **Vote for the week's board game!**\nReact with the emoji to vote:\n"

          i=0
          while read -r GAME; do
            MSG+="${EMOJIS[i]} ${GAME}\n"
            i=$((i + 1))
          done <<< "$GAMES"

          # Send poll message to Discord
          RESPONSE=$(curl -s -X POST "https://discord.com/api/v10/channels/${{ secrets.DISCORD_CHANNEL_ID }}/messages" \
            -H "Authorization: Bot ${{ secrets.DISCORD_BOT_TOKEN }}" \
            -H "Content-Type: application/json" \
            -d "{\"content\": \"${MSG}\"}")

          echo "$RESPONSE" > .poll_response.json
          MESSAGE_ID=$(jq -r '.id' .poll_response.json)
          echo "MESSAGE_ID=$MESSAGE_ID" >> $GITHUB_ENV

      - name: Add reactions to poll message
        run: |
          GAMES=$(jq -r '.[]' games/index.json)
          EMOJIS=(🇦 🇧 🇨 🇩 🇪 🇫 🇬 🇭 🇮 🇯 🇰 🇱 🇲 🇳 🇴 🇵 🇶 🇷 🇸 🇹 🇺 🇻 🇼 🇽 🇾 🇿)
          i=0
          while read -r _; do
            EMOJI_URLENCODED=$(printf "%s" "${EMOJIS[i]}" | jq -sRr @uri)
            curl -s -X PUT "https://discord.com/api/v10/channels/${{ secrets.DISCORD_CHANNEL_ID }}/messages/${MESSAGE_ID}/reactions/${EMOJI_URLENCODED}/@me" \
              -H "Authorization: Bot ${{ secrets.DISCORD_BOT_TOKEN }}"
            i=$((i + 1))
          done <<< "$GAMES"

      - name: Save poll message ID to file
        run: echo "${MESSAGE_ID}" > .poll_id

      - name: Commit and push .poll_id
        run: |
          git config --global user.name "poll-bot"
          git config --global user.email "bot@example.com"
          git add .poll_id
          git commit -m "Save poll message ID"
          git push
